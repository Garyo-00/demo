# 05. 作業実績入力（QR読み取り後）（/workadjust/actual-input）

> ステータス：ドラフト（デモ実装ベース）／要レビュー

## 1. 概要
- 目的：現場でQRを読み取った協力会社の作業員／職長が、当日の作業実績（作業人数・工数）を入力・送信する。
- 利用ロール：協力会社の作業員／職長。
- アクセス経路：QR読み取り（[04.QR発行]）→ 独立ページ（モバイル想定）。

## 2. 画面要素
- ビュー切替（デモ）：アカウントあり／アカウントなし。
- ヘッダー：工事名／作業実績入力／作業日（＋アカウントあり時は会社名）。
- 本体：
  - **アカウントなし**：会社名を選択 → 実績入力フォーム。
  - **アカウントあり**：会社選択を省略しログイン会社の実績入力へ直行。
- 送信後：完了画面（ブラウザを閉じる案内）。

## 3. 表示項目 / 入力項目
- 会社カード：会社名、入場人数（DNN連携値・想定）。
- 作業行：職種／作業内容、通常作業・早出/残業ごとに 作業人数（予定＝表示）／作業人数（実績＝入力）／工数（入力）。

## 4. 機能・操作
- 会社選択（アカウントなし）／会社選び直し。
- 実績入力→送信→完了。

## 5. 状態・ロール別表示差
- アカウント有無で会社選択の要否が変わる。

## 6. データ・外部連携
- 対象作業の予定：作業予定一覧（当日・当該会社）。
- 入場人数：DNN（出面・日報管理）。
- 送信先：作業予定の実績（[01] の実績と同一データ・想定）。

## 7. バリデーション・制約
- （想定）実績人数・工数は0以上。

## 8. 例外・エッジケース
- 対象日に作業予定が無い会社。
- 二重送信・再入力（送信後の修正）。
- オフライン時の送信。

## ⚠️ 9. 未決事項・考慮不足（要確認）
- [ ] ⚠️ **本人特定/認証**：アカウントなしで会社選択のみは、なりすまし・誤会社選択のリスク。認証要否。
- [ ] ⚠️ 「アカウントあり/なし」の本番仕様（デモはトグル）。ログイン会社の判定方法。
- [ ] ⚠️ 対象作業日の決定（QRパラメータ／当日固定）。
- [ ] ⚠️ 送信後の修正・再送・締め（何時まで入力可か）。二重送信防止。
- [ ] ⚠️ [01]元請の実績入力と本画面の実績の**単一ソース化**（どちらが正、上書きルール）。
- [ ] ⚠️ 入場人数（DNN）と入力実績の整合チェック（超過時の警告等）。
- [ ] ⚠️ オフライン/通信断時の一時保存・再送。
- [ ] ⚠️ 個人情報・端末（現場共用端末）でのセッション扱い。
